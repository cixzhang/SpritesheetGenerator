
const tools = {
  PAN: 'pan',
  DRAW: 'draw',
};

const allTools = [
  tools.PAN,
  tools.DRAW,
];

const panels = {
  DETAILS: 'details',
  FILES: 'files'
};

const allPanels = [
  panels.DETAILS,
  panels.FILES,
];

module.exports = {
  tools,
  panels,
  allTools,
  allPanels,
};
